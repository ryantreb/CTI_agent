# ============================================
# CTI Agent v2.4.0 API Key Configuration
# ============================================
# Copy this file to config/.env and fill in your keys.
#
# 13 of 23 MCP servers require NO API keys and work immediately.
# The keys below unlock additional enrichment sources.

# === RECOMMENDED (Free tier available) ===
VT_API_KEY=                # VirusTotal / Google Threat Intelligence (free: 1000 req/day)
OTX_API_KEY=               # AlienVault OTX (free, generous limits)
ABUSEIPDB_API_KEY=         # AbuseIPDB (free: 1000 req/day)
SHODAN_API_KEY=            # Shodan (free: 100 queries/month)

# === OPTIONAL (Free tier available) ===
CENSYS_API_ID=             # Censys (free: 250 queries/month)
CENSYS_API_SECRET=         # Censys API secret

# === PAID (Subscription required) ===
FEEDLY_ACCESS_TOKEN=       # Feedly Threat Intelligence (requires TI subscription)
MALLORY_API_KEY=           # Mallory real-time threat intel

# === NO API KEYS REQUIRED (13 servers) ===
# These work immediately with no configuration:
#   Intelligence:  mcp-threatintel (GreyNoise, abuse.ch), mcp-security-orkl, ti-mindmap-hub-mcp
#   Vulnerability: mcp-nvd, epss-mcp, kev-mcp, vulnerability-intelligence-mcp, nuclei-mcp
#   OSINT:         mcp-dnstwist, networksdb-mcp
#   Utility:       cyberchef-api-mcp
#   Malware:       ghidra-mcp, yara-mcp, capa-mcp, radare2-mcp, binwalk-mcp (Docker)

# === PROXY (Optional) ===
# HTTP_PROXY=
# HTTPS_PROXY=
